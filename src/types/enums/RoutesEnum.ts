enum RoutesEnum {
  ROOT = '',
  LOGIN = 'login',
  REGISTER = 'register',
  DASHBOARD = 'dashboard',
  CATEGORIES = 'categories',
  ORDERS = 'orders',
  PRODUCTS = 'products',
  USERS = 'users',
}

export default RoutesEnum
